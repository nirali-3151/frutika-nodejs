const {AddAddressController} = require('./AddAddress')
const {DeleteAddressController} = require('./DeleteAddress')
const {UpdateAddressController} = require('./UpdateAddress')

module.exports = {
    AddAddressController,
    DeleteAddressController,
    UpdateAddressController,
}